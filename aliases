# Unix
alias tlf='tail -f'
alias tlfn='tail -f -n 1000'
alias mkdir='mkdir -p'
alias ...='../..'
alias l=ls
alias e='$EDITOR'

# Navigator Installer
alias navbuild='builder build navigator.xml linux-x64'
alias gonav='bundle exec boss go server --number=1 --config=Bossfile'

# Bundler
alias b='bundle'

# Rails
alias migrate="rake db:migrate db:rollback && rake db:migrate"
alias m="migrate"
alias s="rspec"

[[ -f ~/.aliases.local ]] && source ~/.aliases.local
